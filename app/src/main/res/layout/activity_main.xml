<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="comsci.teenoyzz.myapplication.MainActivity"
    android:background="@drawable/bg27">

    <TextView
        android:text="ป้ายจราจร"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:layout_centerHorizontal="true"
        android:textSize="36sp"
        android:textColor="@color/colorAccent" />

    <Button
        android:text="@string/MoreInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="onClickMoreInfo"
        android:id="@+id/btnMoreInfo"
        android:layout_alignParentBottom="true"
        android:textSize="18sp" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/livTraffic"
        android:layout_below="@+id/textView"
        android:layout_above="@+id/btnMoreInfo"
        android:background="#a5f9f8f8"
        android:layout_margin="@dimen/my_margin" />

</RelativeLayout>
